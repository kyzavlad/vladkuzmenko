"use client";

// Это временный тестовый код для проверки
export function PersonalBrandHero() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100vh',
      backgroundColor: '#000033', // темно-синий
      color: 'white',
      fontSize: '2rem'
    }}>
      Это тестовая страница PersonalBrandHero. Ошибки быть не должно.
    </div>
  );
}
