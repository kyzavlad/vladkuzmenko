name: Ecosystem redesign visual QA

on:
  push:
    branches:
      - redesign/audience-first-ecosystem-20260815

permissions:
  contents: read

jobs:
  visual-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install project dependencies
        run: npm ci
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.54.2
          npx playwright install --with-deps chromium
      - name: Build static site
        run: npm run build
      - name: Serve static export with clean URLs
        run: |
          npx --yes serve@14 out -l 4173 >/tmp/vk-site.log 2>&1 &
          for i in {1..45}; do
            if curl -fsS http://127.0.0.1:4173/ru >/dev/null; then
              echo "Static site is ready"
              break
            fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4173/ru >/dev/null
          curl -fsS http://127.0.0.1:4173/ru/products >/dev/null
      - name: Capture responsive screenshots and runtime checks
        env:
          BASE_URL: http://127.0.0.1:4173
        run: node scripts/redesign-visual-check.mjs
      - name: Upload visual QA artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ecosystem-redesign-visual-qa
          path: redesign-visual-artifact
          if-no-files-found: warn
          retention-days: 7
